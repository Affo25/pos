# Copy to Vercel → Project → Settings → Environment Variables (Production + Preview).
# CRA inlines REACT_APP_* at build time — redeploy after changes.
# Local dev: put this file in frontend/, save, then restart `npm start` (env is read at startup).

# Production (Vercel, etc.): REACT_APP_API_URL=https://your-api.up.railway.app

# Local dev — default: direct API at http://127.0.0.1:5000 (no proxy; backend CORS allows localhost:3000).
# If your API runs only on Railway (no local Node): set REACT_APP_API_URL=https://your-api.up.railway.app
# Optional proxy mode: REACT_APP_API_URL=proxy → browser calls /api, CRACO devServer proxies (customize-cra-config.js)
# Other local API port: REACT_APP_API_URL=http://127.0.0.1:8080
# REACT_APP_PROXY_TARGET=http://127.0.0.1:5000

# Optional — only if features are used
REACT_APP_PIXABAY_API_KEY=
REACT_APP_FIREBASE_API_KEY=
REACT_APP_BASE_URL=
REACT_APP_API_ENDPOINT=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_DATABASE_URL=
REACT_APP_FIREBASE_PROJECT_ID=
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=
REACT_APP_FIREBASE_APP_ID=
REACT_APP_FIREBASE_MEASUREMENT_ID=
REACT_APP_GOOGLE_MAP_KEY=
REACT_APP_AUTH0_DOMAIN=
REACT_APP_AUTH0_CLIENT_ID=
